import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sunlight.app',
  appName: 'sunlight',
  webDir: 'dist/sunlight',
  bundledWebRuntime: false
};

export default config;
